/**
 * Chat 页 mock 数据：欢迎语、快捷提问、AI 回复池。
 *
 * 纯前端演示模式——不接任何后端接口，
 * AI 回复由 pickMockReply() 按关键词匹配后 setTimeout 返回。
 */

/** 初始 AI 欢迎语 */
export const WELCOME_MESSAGE =
  '你好，我是你的学科辅导助手 🌊\n可以问我知识点、让我解析错题，或者从左侧快速引用里挑一条开始～'

/** 快捷提问（点击直接发送） */
export const QUICK_QUESTIONS: string[] = [
  '数列求和有哪些常用方法？',
  '怎么判断复合函数的单调性？',
  '帮我制定一份本周复习计划',
  '错题总是重复错怎么办？',
  '等差数列和等比数列怎么区分？',
]

/* ============ AI 回复池（markdown） ============ */

const REPLY_COMPOUND_FUNCTION = `**复合函数单调性**的判断核心是「同增异减」原则：

1. **拆分内外层**：设 u = g(x)，则 y = f(u)
2. **分别判断单调性**：确定内层 g(x) 与外层 f(u) 在对应区间上的增减性
3. **合成结论**：内外同增或同减 → 复合函数递增；一增一减 → 复合函数递减

⚠️ **易错点**：一定先求定义域！内层函数的值域必须落在外层函数的定义域内。

**示例**：y = √(x² - 2x)，先看定义域 x² - 2x ≥ 0，再判断内层 u = x² - 2x 的单调区间，最后按同增异减合成。`

const REPLY_ERROR_PARSE = `好的，我来帮你分析这道错题 📝

**解题思路**：
1. 先明确题目考查的核心知识点
2. 回顾相关概念与公式，写下已知条件
3. 对照你的解法，定位出错的步骤

**错因分析**：从你标记的错因来看，建议回到课本对应章节把基础概念再过一遍，然后做 2-3 道同类题巩固。

**复习建议**：把这道题加入周末复盘清单，3 天后重做一遍，检验是否真正掌握。

> 这是演示模式的模板回复，接入 LLM 后会针对题目内容逐步解析 🌊`

const REPLY_PLAN = `根据你的情况，给你一份**复习计划建议**：

- **今天**：把错题本里标记「方法不会」的题目优先过一遍（约 25 分钟）
- **明天**：针对掌握度低于 50% 的知识点做专项练习
- **本周内**：完成一次完整复盘，检验薄弱点是否补齐

💡 建议用番茄钟把学习拆成 25 分钟一段，中间休息 5 分钟，效率更高。`

const REPLY_SUM_METHOD = `**数列求和**的常用方法有这几种：

1. **公式法**：等差 / 等比数列直接套前 n 项和公式
2. **错位相减法**：适用于「等差 × 等比」型，注意对齐项数
3. **裂项相消法**：把通项拆成两项之差，前后抵消
4. **分组求和法**：把数列拆成几个能直接求和的子数列

⚠️ 错位相减最容易在「项数对齐」上出错，建议写完检查首末项。`

const REPLY_DEFAULT = '这是演示模式，实际接入后会由 LLM 实时生成 🌊\n\n可以试试问我：**复合函数**、**数列求和**、**复习计划**，或者发一道**错题**让我解析。'

/**
 * 按关键词匹配一条 mock 回复。
 * 优先级：复合函数 > 错题/解析 > 计划 > 数列求和 > 默认
 */
export function pickMockReply(input: string): string {
  const text = input.toLowerCase()
  if (text.includes('复合函数')) return REPLY_COMPOUND_FUNCTION
  if (text.includes('错题') || text.includes('解析')) return REPLY_ERROR_PARSE
  if (text.includes('计划')) return REPLY_PLAN
  if (text.includes('数列求和')) return REPLY_SUM_METHOD
  return REPLY_DEFAULT
}
