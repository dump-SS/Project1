[project]
name = "epochx-state-engine"
version = "0.1.0"
description = "学习状态量化引擎（PRD 5.2 确定性计算层）"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8", "pytest-cov"]

[tool.pytest.ini_options]
testpaths = ["tests"]
