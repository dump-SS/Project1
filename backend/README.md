# EpochX 后端（FastAPI + SQLAlchemy + SQLite）
# 严格按 docs/openapi.yaml 实施，详见 docs/api-design-unified.md
#
# 快速开始：
#   python -m venv .venv
#   .venv\Scripts\Activate.ps1
#   pip install -r requirements.txt
#   uvicorn main:app --reload --port 8000
#
# 之后打开 http://localhost:8000/docs 看 Swagger UI
