# 学习状态智能助手 PRD v1.3（MVP：第一板块 · 中学学情导学与状态评估）

- 文档状态：待评审
- 版本：v1.3
- 日期：2026-08-15
- 覆盖范围：产品整体三大板块概览 + 第一板块 MVP 详细设计（初中 + 高中）
- 后续板块（第二、第三板块）本版仅作规划性描述，不作为 MVP 交付范围

变更记录：
- v1.3：版本号统一调整至 v1.3（内容同 v1.1，无新增变更）。
- v1.1：确定接入第三方大模型 API。新增第 6 节「AI 能力架构与大模型接入」；5.2 状态量化由纯规则改为「规则计算 + AI 动态调权（受限区间）」；5.3 建议生成由规则模板改为「LLM 生成 + 安全审核层」；新增 5.4「学习总结与复盘」；相应更新数据模型、隐私与非功能需求、指标与开放问题。原第 6-10 节顺延为第 7-11 节。新增第 0 节「与市面现有产品的差异与优势」，前置阐述差异化定位。
- v1.0：初版，三板块概览 + 板块一 MVP 详细设计（纯规则算法方案）。

---

## 0. 与市面现有产品的差异与优势

本节用于对外沟通与对内定位，说明本产品为什么不与现有工具直接竞争、而是占据一个目前空白的位置。所列市面产品特征基于公开信息的概括，具体竞品的功能与定价以各自官网为准，建议在对外使用前做一轮核实。

### 0.1 市面现有产品的类型与盲区

目前面向基础教育学习场景的产品大致分为四类，各自解决了部分问题，但都没有覆盖"学习状态"这个维度：

| 类型 | 代表形态 | 解决的问题 | 盲区 |
|---|---|---|---|
| 题库/拍搜类 | 拍照搜题、智能题库、刷题 App | 找到题、对答案、看解析 | 只关注"题做对了没有"，不关注"这个学生现在状态适不适合继续学" |
| 课程/辅导类 | 网课、1 对 1、AI 课 | 内容讲解与知识传递 | 内容是核心资产，学生的情绪、疲劳、节奏不在其闭环内 |
| 时间管理/计划类 | 番茄钟、待办清单、学习计划 App | 安排时间、打卡提醒 | 按时间表执行，不管执行时的实际状态，计划不会随状态自适应调整 |
| 通用 AI 助手 | 大模型对话类产品 | 答疑、讲解、通用学习建议 | 无长期学习数据积累，每次对话从零开始，建议无上下文、无连续性 |

**共同盲区**：这四类都以"内容"或"时间"为中心，没有一类以"学习者当下的状态"为中心。而状态（专注度、疲劳、情绪的动态变化）恰恰是影响学习效率最直接的变量，也是家长和学生在分数之外最缺乏客观感知的部分。

### 0.2 本产品的差异化定位

本产品的切入点不是再做一个题库或课程，而是做**学习状态的感知与规划中枢**，与上述产品形成互补而非替代：

- **状态量化是核心资产，不是附加功能**。市面产品的"学习报告"多为事后统计（做了多少题、正确率多少），本产品的状态分是行为数据+自评的动态量化，并且随时间形成趋势，用于**事前**调整规划，而非仅用于事后展示。
- **规划随状态自适应，而非固定模板**。计划类产品的时间表是静态的，本产品的导学规划会根据最近一次状态评估动态调整任务强度——状态差时降量保节奏，状态好时适度加量，这是"以状态为中心"与"以时间为中心"的本质区别。
- **AI 负责表达与适配，规则负责事实与底线**。相比通用 AI 助手"每次从零开始"，本产品把模型调用限定在有长期数据积累的上下文里（历史状态、权重、目标进展），建议和复盘有连续性；同时状态分等关键事实由确定性规则计算，不交给模型，避免了通用 AI 在数字和判断上的不可靠。

### 0.3 三板块组合的纵深

单一功能容易被模仿，本产品的长期优势在于三板块组合形成的数据与能力纵深，这是市面上单一类型产品难以复制的：

- **板块一（本版）**建立状态感知能力与用户信任，是入口。
- **板块二**把状态与具体学科内容打通（接入本地知识库、长期学习画像），形成"既懂你的状态、又懂你学的内容"的组合——市面上做内容的不懂状态，做状态（若有）的不懂内容。
- **板块三**在合规前提下做匿名群体参照，让个体的状态评估有了"同龄人/同目标群体"的横向坐标，进一步加深数据壁垒。

**需要坦诚的一点**：上述优势是定位层面的判断，不是已验证的事实。状态量化是否真比"凭感觉"更能帮到学生、AI 调权是否真比固定权重更准，都需要 MVP 上线后用数据验证（见第 9 节指标与第 11 节开放问题）。本节的对外表述应避免过度承诺，尤其在涉及"提升成绩"类效果宣称时，需遵守广告法与教育类产品的宣传规范。

---

## 1. 背景与问题定义

基础教育阶段（尤其中学）学生普遍存在以下问题：

- 学习前缺乏系统规划，凭感觉安排时间和内容，效率低。
- 学习状态（专注度、疲劳度、情绪、掌握程度的变化趋势）缺乏客观感知，学生和家长往往只能凭"感觉"或考试分数事后判断，反馈滞后。
- 学习建议普遍是通用型内容（如"多刷题""该复习了"），没有结合个人实际学习内容、进度和当下状态。

产品目标：用 AI 把"学习规划 - 状态感知 - 个性化建议"串成一个闭环，让学生学习前有方向、学习中有反馈、学习后有调整依据。

## 2. 产品整体架构（三大板块）

产品分三大板块，逐步建设，本 PRD 仅对板块一做完整设计，板块二、三做方向性说明以确保架构不冲突。

| 板块 | 定位 | 核心能力 | 本版状态 |
|---|---|---|---|
| 板块一 | 学习状态与规划中枢（面向基础教育） | 导学规划、状态动态量化（AI 调权）、个性化学习建议、学习总结复盘 | **MVP，本 PRD 详细设计** |
| 板块二 | 垂直学科落地场景 | 分学科/专业的知识归纳与复盘，接入本地知识库，承载用户长期学习画像 | 规划中，方向性描述 |
| 板块三 | 群体参照 | 匿名特征值上传，跨用户群体状态/方法对比参照，不上传原始学习内容 | 规划中，方向性描述 |

三个板块的数据关系：板块一产生的"状态量化结果"和"学习画像片段"是板块二长期用户画像的基础输入之一；板块三只消费板块一/二产出的**匿名化统计特征**，不接触原始内容，三者在数据边界上要在架构上提前预留隔离层（见 10.3）。

高等教育阶段的板块一延伸暂不在本版规划，待中学阶段模型验证后再设计（高等教育学习自主性、学科专业化程度更高，评估维度和导学逻辑需要单独调研，不能直接套用中学模型）。

## 3. MVP 范围定义

- **覆盖学段**：初中、高中（不含小学、不含高等教育）。理由：中学阶段学习自主性和目标明确性介于小学和大学之间，行为数据+自评的组合方案适配度最高，且是 K12 辅导市场主流需求。
- **产品形态**：Web + 小程序，不做原生 App。理由：MVP 阶段优先验证算法和体验假设，Web/小程序开发和分发成本低，可快速迭代；原生 App（含日常打卡、状态感知的系统级能力）留作后续版本规划。
- **MVP 只做板块一**，包含四个功能模块：
  1. 学习前导学规划
  2. 学习状态动态量化评估（含 AI 动态调权）
  3. 个性化学习建议（LLM 生成）
  4. 学习总结与复盘（LLM 生成，状态与规划维度）
- **AI 接入方式**：调用第三方云端大模型 API，不自行训练或微调模型。模型负责表达与判断倾向，不负责计算事实，详见第 6 节。

不在 MVP 范围内：本地知识库接入、学科知识内容复盘（板块二）、群体匿名对比（板块三）、高等教育适配、模型自训练/微调。

## 4. 用户与场景

### 4.1 目标用户

- 主用户：初中生、高中生（13-18 岁），自主使用产品进行日常学习管理。
- 关键相关方：家长（可能有查看/监督需求，但本版不做家长端，仅预留数据接口可能性）、学校/教师（暂不作为 MVP 用户角色）。

因用户群体为未成年人，产品在数据采集、隐私保护、内容安全上需要更严格的默认设置，具体见第 8.1 节（隐私与未成年人保护）和 6.2（AI 数据出域边界）。

### 4.2 典型场景

1. 小凡（高二）晚自习前打开产品，希望知道今晚该先复习什么、花多久——触发"导学规划"。
2. 小凡做完一轮数学练习后，手动录入本次练习的正确率和用时，产品结合页面失焦次数等行为数据，以及他自己勾选的"有点烦躁/一般专注"自评，更新他的学习状态，并在后续规划里做调整——触发"状态动态量化"。
3. 系统发现小凡最近三天英语单词记忆正确率持续下降且自评疲劳度上升，主动提示调整复习节奏或建议短时高频而非长时低效——触发"个性化建议"。

## 5. 功能需求详细设计

### 5.1 学习前导学规划

**目标**：学生打开产品时，能在 1-2 分钟内获得一份"今天/本阶段该学什么、怎么学、学多久"的可执行规划，而不是空白页面发呆。

**输入**：
- 学段、年级、学科（用户设置，一次性）
- 短期目标（如"两周后期中考试""本周弄懂函数章节"）与长期目标（如"高考目标分数区间"），用户手动输入或从预设模板选择
- 当前可用学习时间（用户输入，如"今晚 2 小时"）
- 历史学习数据（若有）：最近的状态量化结果、已完成任务、薄弱知识点标记

**处理逻辑（MVP 简化版，非板块二的知识库深度诊断）**：
1. 若无历史数据（新用户）：基于用户填写的目标和年级学科，套用规则模板生成初版计划（如"目标是期中考数学 120+，本周安排：函数专项 3 次、综合练习 2 次"）。
2. 若有历史数据：结合最近一次状态量化结果（见 5.2）动态调整任务量和难度——状态偏疲劳/低效时降低任务强度、延长单次任务间隔；状态偏高效稳定时可适度提高强度或引入拔高内容。
3. 输出结构化计划：任务列表（学科、内容方向、预计时长、优先级），非具体题目内容（题目内容归属板块二知识库能力，MVP 不做）。

**交互要点**：
- 用户可对生成的计划进行手动调整（改时长、删减任务），调整行为本身也作为后续算法的反馈信号记录。
- 计划完成情况需要用户在学习后简单确认（完成/部分完成/未完成），作为下一次规划和状态量化的输入。

**MVP 边界**：不做具体知识点内容推送、不做题库、不做智能出题，只做"任务级"规划，内容颗粒度到"学科+方向+时长"。

### 5.2 学习状态动态量化算法

**目标**：把学生一次学习活动或一段时间（如一天/一周）的状态变化，转化为可比较、可追踪的量化指标，作为规划调整和建议生成的依据，而不是黑箱评分。

**数据来源（行为数据 + 自我报告结合）**：

| 类型 | 数据项 | 采集方式 |
|---|---|---|
| 行为数据 | 学习时长、任务完成度（完成/部分/放弃）、练习正确率（若有客观测验/打卡）、学习节奏（是否按计划时段学习、中断次数、切出应用次数——小程序场景下可采集页面失焦次数作为弱信号） | 系统自动记录，用户无感 |
| 自我报告 | 每次学习结束后的轻量自评：专注度（1-5）、疲劳度（1-5）、情绪状态（正向/中性/负向的简单三选）、对本次内容难度的主观感受（偏易/适中/偏难） | 用户主动填写，控制在 10 秒内完成，避免造成填报负担 |

**算法设计（规则计算为主体 + AI 动态调权，分数计算过程本身保持确定性可复现）**：

1. **单次学习状态分**：对行为数据和自评数据分别标准化（0-1），按当前权重加权得到单次状态分。计算公式固定，权重可由 AI 调整（见第 4 点）。
   - 行为子分 = w1×完成度 + w2×正确率（若无正确率数据则该项权重归零并重新分配） + w3×节奏稳定度（中断次数的反向标准化）
   - 自评子分 = w4×专注度 + w5×(6-疲劳度) + w6×情绪正向程度
   - 单次状态分 = α×行为子分 + β×自评子分（初始 α=β=0.5，各 w 初始等权；所有权重存于后台配置与用户级权重表，不写死在代码逻辑里）
2. **趋势量化**：以滑动窗口（如最近 7 次学习记录或最近 7 天）计算状态分的均值和斜率（简单线性回归斜率即可，不需要复杂时序模型），得到"当前状态水平"和"变化趋势（上升/持平/下降）"两个维度。
3. **状态标签化**（供用户直观理解，避免只给一个抽象数字）：将（水平，趋势）二维结果映射为几类状态标签，例如：
   - 高效稳定：水平高 + 趋势平或升
   - 疲劳预警：水平中低 + 疲劳自评持续偏高 + 趋势降
   - 情绪受阻：情绪自评连续偏负 + 正确率/完成度下降
   - 波动上升期：水平中等但趋势明显上升
   - 数据不足：记录次数低于最小阈值（如 3 次）时不下结论，仅展示原始记录
   - 注：上述标签判定中"水平高/中低""趋势明显上升"等词对应的具体数值区间（如状态分阈值、回归斜率阈值），待算法实现阶段基于初始权重和真实数据分布校准确定，本 PRD 不预设具体数值，避免无数据依据的凭空设定。
4. **AI 动态调权（本版核心能力）**：由大模型分析用户历史数据，输出权重调整建议，但调整幅度受硬限制区间约束，模型不能直接改写最终权重。
   - **触发时机**：按周期离线批量执行（如每周一次或累计满 N 条新记录时），不在用户单次学习后实时调权——避免单次异常数据导致权重抖动，也便于控制 API 成本。
   - **输入**：该用户近期的结构化特征序列（各维度标准化值、状态分、趋势、计划完成情况、用户对建议的反馈），以及当前权重值。
   - **输出**：各权重的建议值 + 调整理由（理由必须返回，用于 7.3 可解释性和问题回溯）。
   - **硬限制（必须在代码层强制校验，不依赖提示词约束）**：
     - 每个权重有允许区间，如 α、β ∈ [0.3, 0.7]，各子项权重 ∈ [0.1, 0.5]，且同组权重之和归一化为 1。
     - 单次调整幅度上限（如任一权重单次变动不超过 0.1），防止突变造成用户状态分跳变。
     - 模型返回值越界、格式非法、字段缺失或 API 调用失败时，**一律回退到当前权重**（不是回退到初始值，避免丢失已积累的校准结果），并记录异常日志。
   - **每次调权需留痕**：记录调整前后权重、模型给出的理由、生效时间，支持回溯与人工回滚。
   - **用户可见性**：不向用户暴露权重数值和公式（延续 5.2 输出原则），但用户可在设置中关闭"AI 自动调整"，改用固定默认权重，保留用户的最终控制权。
   - **不做**：不做跨用户的联合训练或模型微调（涉及未成年人数据，MVP 阶段不触碰）；权重调整仅在单用户维度内进行。

**输出**：
- 给用户的呈现：状态标签 + 简要说明（不展示原始权重公式，避免用户过度纠结"分数"本身，弱化应试化倾向）
- 给内部系统的呈现：结构化状态分和趋势数据，作为 5.1 规划调整和 5.3 建议生成的输入参数

**冷启动处理**：新用户前几次记录数据不足，状态量化模块不强行输出结论，仅提示"数据积累中"，避免早期误判影响体验。

**异常与边界场景**（具体处理策略由实现阶段定义，此处仅列出需覆盖的场景避免遗漏）：
- 数据断层：用户连续多天（如超过滑动窗口长度）未产生学习记录，窗口内数据过旧时如何处理——是否重置趋势计算、是否提示"重新开始记录"。
- 记录删除回溯：用户手动删除部分学习记录后，已生成的状态评估是否回溯重算，还是仅影响后续计算。
- 极端自评值：用户连续多次全部选择极端值（如疲劳度全部满分）时，是否触发异常数据标记而非直接纳入计算。

**MVP 不做**：不做跨学科的加权综合（不同学科状态先分开评估，避免语文情绪和数学正确率强行合并成一个分数导致语义混乱）；不自行训练或微调模型（仅调用第三方 API）；不让模型直接产出状态分本身（分数始终由固定公式计算，模型只影响权重）。

### 5.3 个性化学习建议

**目标**：结合 5.1 的计划执行情况和 5.2 的状态量化结果，以及用户实际学习的学科/内容方向，给出具体、可执行、非通用模板的建议。

**触发时机**：
- 单次学习结束后（即时反馈，1-2 条简短建议）
- 阶段性回顾（如每周一次，汇总本周状态趋势给出调整建议）

**建议生成逻辑（LLM 生成 + 安全审核层，规则兜底）**：

1. **结构化上下文组装**：由规则层先算出确定性结论（状态标签、趋势、计划完成情况、薄弱学科方向），组装成结构化上下文，而不是把原始数据一股脑丢给模型。
2. **LLM 生成建议**：模型基于上下文生成具体、口语化、贴合该学生实际学习内容的建议，解决 v1.0 规则模板"话术固定、久用即腻"的问题。
3. **安全审核层过滤（强制，不可绕过）**：生成内容展示给用户前必须过审，详见 6.3。
4. **规则兜底**：API 超时/失败/审核不通过时，回退到按"状态标签 + 学科 + 计划执行情况"匹配的建议模板库，保证用户始终能拿到可用建议，不出现空白。兜底模板库仍需维护，示例：
   - 状态=疲劳预警 + 学科=数学 + 最近正确率下降 → 建议缩短单次时长、增加间隔、暂缓新内容转为已掌握内容的巩固练习
   - 状态=情绪受阻 + 计划连续两次未完成 → 建议主动降低下一次任务量，并提示"降低目标不等于放弃目标"，避免打击积极性
   - 状态=高效稳定 + 趋势上升 → 建议适度增加拔高内容或提前进入下一阶段目标

**边界与安全**：
- 建议内容不涉及心理诊断或医疗建议，涉及情绪相关表述使用鼓励、中性、非评判性语言（如不说"你很焦虑"，而说"最近感觉有点吃力，我们把节奏放慢一点"）。
- 若自评数据连续显示强烈负向情绪信号（如连续多次选择负向情绪且伴随疲劳度满分），MVP 阶段不做心理干预建议，仅温和提示"如果最近学习压力比较大，也可以和老师或家长聊一聊"，不越界扮演心理咨询角色。
- LLM 生成的建议同样受上述两条约束，通过提示词约定 + 安全审核层双重保证（提示词只是第一道，不能作为唯一防线）。

### 5.4 学习总结与复盘（LLM 驱动）

**目标**：把一段时间内零散的学习记录和状态变化，整理成学生看得懂、用得上的复盘内容，而不是让用户自己看一堆数据图表。

**范围说明**：本节是板块一内的"状态与规划维度"总结（我学得怎么样、节奏对不对、下阶段怎么调）。板块二的"学科知识内容归纳复盘"（具体知识点掌握、错题归因）不在本版范围，两者边界见 10.1。

**触发方式**：
- 周期性自动生成：每周一次周复盘，学习记录数达标才生成（不足则提示数据不够）。
- 用户主动触发：用户可手动要求生成某个时间段的总结，需做频率限制（如每日上限若干次）以控制 API 成本。

**输入**：该周期内的学习记录、状态分与趋势、状态标签变化、计划完成情况、目标进展，以及用户填写的目标描述文本。

**输出内容结构**（固定框架，避免模型自由发散导致每周格式不一）：
- 这段时间的状态概览（用自然语言解释状态标签变化，而非只给分数）
- 观察到的规律（如"数学安排在晚上时完成度明显低于下午"）
- 下阶段建议（1-3 条，可直接落到 5.1 的规划调整）
- 一句鼓励性收尾（语气约束同 5.3）

**质量与安全约束**：
- 总结必须基于真实数据，禁止编造未发生的学习行为或不存在的进步（幻觉风险是本功能最主要的质量风险）。实现上采取：仅传入结构化数据 + 要求模型引用具体数据点 + 审核层校验关键数字与数据库是否一致。
- 数据不足时不硬凑总结，明确告知"本周记录较少，暂不生成完整复盘"。
- 生成内容同样经 6.3 安全审核层。

**用户反馈**：总结页提供"准不准/有没有用"的轻量反馈入口，作为后续优化提示词和调权策略的依据。

## 6. AI 能力架构与大模型接入

本节统一定义大模型的接入方式、数据边界和安全机制，5.2、5.3、5.4 三处 AI 能力均遵循此约定。

### 6.1 能力分工

| 能力点 | 模型职责 | 确定性部分（不交给模型） | 失败回退 |
|---|---|---|---|
| 状态量化调权（5.2） | 分析历史特征，建议权重值 + 理由 | 状态分公式计算、权重区间校验、标签判定 | 保持当前权重 |
| 个性化建议（5.3） | 生成贴合个人情况的建议文案 | 状态标签与触发条件的判定 | 规则模板库 |
| 学习总结复盘（5.4） | 组织自然语言复盘、总结规律 | 数据统计、趋势计算、数字校验 | 提示稍后再试，不展示半成品 |

原则：**模型负责表达和判断倾向，不负责计算事实**。所有对用户可见的数字和结论性标签都来自规则层，模型不得覆盖。

### 6.2 数据出域边界（重要）

MVP 采用第三方云端大模型 API，涉及数据出域，需明确边界：

**允许传输**：
- 结构化脱敏特征（各维度标准化值、状态分、趋势、标签、计划完成情况）
- 用户自行填写的文本内容（学习目标描述、任务备注等）——经产品决策允许传输，以提升建议与总结的贴合度

**禁止传输**：
- 用户身份信息：真实姓名、手机号、监护人联系方式、学校/班级、账号 ID（如需关联请用随机化的临时会话标识，且不可反查）
- 精确地理位置、设备指纹类信息

**因允许用户文本出域而必须落实的义务（不可省略）**：
- 隐私政策与首次使用告知中**明确写出**："你填写的学习目标等文字内容会发送给第三方 AI 服务用于生成建议"，不能只笼统写"使用第三方服务"。
- 监护人授权环节需覆盖此项数据处理，作为授权内容的一部分明示。
- 输入框旁给出轻量提示，引导学生不要填写个人隐私信息（如家庭住址、联系方式）。
- 选择 API 供应商时确认合同条款：不得用于模型训练、数据留存期限尽可能短、有中国境内合规部署方案（未成年人数据跨境传输的合规风险显著，若供应商无境内节点则不予采用）。
- 对用户输入做敏感信息检测（如手机号、身份证号模式匹配），命中时在发送前脱敏或阻断。
- 提供开关：用户/监护人可选择"不发送我的文字内容"，此时降级为仅用结构化特征生成，功能可用性略降但不阻断。

**已知风险（需产品和法务确认后再上线）**：允许未成年人填写的自由文本出域，即便有上述措施，仍存在学生无意填入敏感信息并被第三方留存的风险。这是本版最需要法务评审的一点，不建议在评审通过前上线该项。

### 6.3 内容安全审核层

所有展示给用户的模型生成内容（5.3 建议、5.4 总结）必须经过审核，审核不通过则回退或不展示：

- **禁止内容**：心理诊断或病理化表述、贬低打击性评价、与学习无关的话题延展、医疗/用药建议、任何鼓励过度学习损害健康的表述（如"再熬一晚就好"）、涉及自我伤害的内容。
- **强制语气**：鼓励、中性、非评判，符合未成年人沟通规范。
- **实现方式**：供应商侧内容安全能力 + 自建关键词/正则黑名单 + 关键数字与数据库一致性校验（针对 5.4 幻觉）三层结合，不依赖单一手段。
- **危机信号识别**：若用户文本或自评数据出现自我伤害等强风险信号，不由模型自由回应，直接切换为固定的、经审定的引导文案（建议联系老师、家长或专业帮助渠道），并记录事件供人工关注。此路径必须硬编码，不走 LLM 生成。
- **人工抽检**：上线初期对生成内容做人工抽样审查，作为提示词与黑名单的迭代依据。

### 6.4 成本、性能与可用性

- **调用时机设计以控成本为前提**：调权按周期批量执行，总结按周生成，建议在单次学习后生成（频次最高，需评估成本，可考虑对低活跃用户降频）。
- **异步化**：所有模型调用异步执行，不阻塞用户主流程（用户不必等生成完才能看计划）；生成完成后以消息/角标提示。
- **超时与重试**：设定超时上限（如 10 秒），失败重试不超过 2 次，仍失败即走回退路径。
- **供应商抽象层**：接入层需对模型供应商做接口抽象，避免与单一厂商强耦合，便于因合规、成本或稳定性原因切换。
- **成本监控**：需有按用户/按功能的调用量与成本统计，并设置总量告警阈值，防止异常调用导致费用失控。

### 6.5 AI 输出的可解释性与留痕

- 每条建议、总结、权重调整都需存储其输入依据和模型返回的理由，用户侧可查看"依据了哪些数据"的简版说明。
- 用户可反馈"这条不准"，反馈与当次输入上下文一并留存，作为提示词与调权迭代的输入。

## 7. 信息架构与数据模型（简要）

核心实体（MVP 范围）：

- `User`：学段、年级、学科列表、目标设定（短期/长期）
- `LearningPlan`：计划所属用户、生成时间、任务列表（学科、方向、预计时长、优先级、状态：待完成/完成/部分完成/放弃）
- `LearningRecord`：单次学习行为数据（时长、完成度、正确率(可空)、中断次数）+ 自评数据（专注度、疲劳度、情绪、难度感知）+ 关联的计划任务(可空，允许自由学习不挂靠计划)
- `StateAssessment`：基于一批 `LearningRecord` 计算得到的状态分、趋势、标签，按学科分别存储
- `Recommendation`：生成的建议内容、触发依据（关联的 StateAssessment 和 LearningPlan）、生成方式（LLM 生成/模板兜底）、用户反馈（有用/一般/没用）
- `UserWeightConfig`：用户级权重表（α、β 及各子项权重当前值）、是否启用 AI 自动调权、最近更新时间
- `WeightAdjustLog`：每次调权留痕——调整前后权重、模型返回的理由、是否发生越界回退、生效时间，支持回溯与人工回滚
- `LearningSummary`：周期性复盘内容、覆盖时间范围、引用的数据点、生成状态（成功/数据不足/生成失败）、用户反馈
- `AICallLog`：模型调用记录——功能类型（调权/建议/总结）、输入摘要、耗时、是否成功、审核结果、成本相关计量，用于 6.4 成本监控和问题排查

数据边界说明：本版数据模型只在用户自己账号下闭环，不涉及匿名化上传（板块三）和本地知识库调用（板块二），为后续板块预留字段扩展空间但不实现。涉及大模型调用的字段需按 6.2 的出域边界设计，`AICallLog` 中不得存储完整的用户身份信息。

## 8. 非功能需求

### 8.1 隐私与未成年人保护（高优先级）

- 目标用户为未成年人，需遵循《未成年人网络保护条例》及《个人信息保护法》相关要求：最小必要原则采集数据、监护人授权机制、提供数据删除/导出入口。涉及第三方 AI 服务的数据处理需单独告知并取得授权，详见 6.2。
- 自评中的情绪、疲劳等数据属于敏感度较高的行为画像数据，需加密存储，明确告知用途，不用于本版之外的商业化画像用途。
- 需要家长/监护人授权流程，MVP 阶段采用简化的监护人邮箱/手机号确认机制，具体规则：
  - 首次使用时需填写监护人联系方式并发送确认请求，监护人点击确认后授权生效。
  - 授权有效期 30 天，到期后需重新确认；期间监护人可通过确认链接中的入口随时撤销授权，撤销后账号进入只读状态（可查看历史数据，不可新增记录），直至重新授权。
  - MVP 阶段无法核验监护人与用户的真实关系（如用户填写自己的邮箱冒充监护人），作为已知风险记录，依赖后续版本引入更强的身份核验手段（如实名认证），本版以流程存在为合规底线。

### 8.2 性能与体验

- 自评填报流程需控制在 10 秒内完成，避免打断学习节奏。
- 状态量化计算可异步进行，不阻塞用户查看学习计划的主流程。
- 大模型调用全部异步，超时与回退策略见 6.4；任何 AI 功能不可用时，产品核心流程（记录学习、查看计划）必须仍然可用。

### 8.3 可解释性

- 状态标签和建议都需要能追溯到"为什么"（对应哪些行为数据/自评数据），避免用户觉得是黑箱评分，产生不信任或抵触情绪。
- 引入 AI 调权后，可解释性要求不降低：权重调整需留痕并可回滚，具体见 6.5。

## 9. 关键指标与验收标准

MVP 上线后重点观察（首版上线以观察趋势为主，不设硬性阈值，待积累 2-4 周真实数据后再确定各指标基线和目标值，避免团队对"什么算成功"产生分歧）：

- 导学规划：计划生成后的采纳率（用户是否直接使用/仅小幅调整 vs 大幅修改或忽略）
- 状态量化：自评填报完成率（体现负担是否可控）、状态标签与用户主观认可度的一致性（可通过简单反馈"这个判断准不准"收集）
- 个性化建议：建议的"有用"反馈占比、建议后的行为改变率（如收到"降低任务量"建议后下一次任务量是否确实调整）
- 留存：连续 7 日、14 日使用率，作为闭环是否形成习惯的核心信号
- AI 能力质量：建议/总结的"有用"与"不准"反馈比例、安全审核层拦截率（拦截率异常高说明提示词有问题）、生成失败与回退触发率
- AI 成本效率：单用户月均 API 成本、各功能调用占比，用于判断商业模型是否成立

验收标准（MVP 可上线的最低要求）：
- 四个模块功能可用，冷启动（新用户前 3 次使用）体验不出现明显断裂或报错
- 状态量化在数据不足时不强行输出误导性结论
- 未成年人数据保护基础机制（监护人授权、数据可删除）落地，不作为后续版本再补
- AI 相关硬性要求：
  - 权重调整越界时确实回退且有日志，不出现状态分跳变
  - 大模型 API 全部不可用时，建议走模板兜底、核心流程不受影响
  - 安全审核层对已知禁止内容（心理诊断、贬低性表述、危机信号）有明确拦截用例并通过测试
  - 6.2 涉及的隐私告知与监护人授权文案已随功能上线，且已通过法务评审（用户自由文本出域是本版最高合规风险项）

## 10. 后续板块规划方向（非本版交付，仅做架构预留说明）

### 10.1 板块二：垂直学科落地场景

- 按场景/学科/专业提供知识内容归纳与复盘能力，接入本地知识库。"本地知识库"的候选技术形态（待板块二立项时评审确定，本 PRD 仅列出可能方向以支撑架构预留判断）：
  - 本地部署的开源 LLM + 私有学科语料微调（隐私最强，算力成本高）
  - 云端通用 LLM + 本地向量数据库做检索增强（RAG），原始内容不出本地（折中方案）
  - 纯本地文件索引 + 规则化知识归纳（无生成式模型，能力受限但实现最简单）
  - 上述形态对数据是否出域、模型推理位置的要求差异很大，直接影响板块一数据模型需要预留的接口形态，因此即便板块二不实现，也建议在 MVP 数据层设计时按"原始内容默认不出本地"的最严格假设做预留。
- 承载用户长期学习画像：整体规划、短长期目标进展会被此处模型调用，即板块一产出的 `StateAssessment`、`LearningPlan`、`LearningSummary` 历史数据是板块二画像的输入源之一。
- 需要单独评估：本地知识库的数据来源、更新机制、不同学科知识结构的差异化建模。
- **与 5.4 的边界**：板块一的 5.4 只做"状态与规划维度"的复盘（学习节奏、状态趋势、计划执行）；板块二做"知识内容维度"的复盘（知识点掌握、错题归因、概念关联）。前者输入是行为与状态数据，后者输入是学习内容本身，需要知识库支撑。实现时注意两者的总结入口在用户侧应有清晰区分，避免用户困惑于"为什么有两个复盘"。
- **模型部署形态的衔接问题**：板块一 MVP 采用云端 API 且允许用户文本出域，板块二若采用本地知识库/本地模型（出于隐私考虑），两者的数据处理策略会不一致。需要在板块二立项时统一决策：是把板块一也迁到本地/私有化部署，还是明确区分"哪类数据走云端、哪类只留本地"。这个问题越晚决策，改造成本越高。

### 10.2 板块三：群体匿名参照

- 只上传匿名特征值（如状态标签分布、某类学习方法的效果统计），不上传任何原始研究/学习内容。
- 需要设计匿名化/脱敏规则和最小群体规模限制（避免小样本群体反推出个体身份），这是该板块能否合规上线的前置条件，需要专门的隐私工程评审，不能等做完了再补。

### 10.3 架构预留建议

即便 MVP 不实现板块二、三，建议在数据层设计上：
- 板块一产出的数据默认不跨用户可见，任何未来的"匿名聚合"都应是显式的、用户可感知的授权动作，不能是默认行为。
- 数据模型中为"匿名特征提取"预留独立的统计视图层，避免后续为了做板块三去改板块一的原始数据结构。
- 大模型接入层做供应商抽象（见 6.4），为后续可能的本地化/私有化部署留出替换空间。

## 11. 开放问题（需要产品后续决策，本版不下结论）

- 高等教育阶段的板块一延伸方案，需要单独调研（自主学习场景、评估维度都可能与中学差异较大）。
- 家长端角色是否在板块一之后单独立项，还是作为板块一的子功能，需要看 MVP 数据反馈和用户诉求。
- AI 调权的效果验证方式：如何证明"AI 调权后的状态评估比固定权重更准"？需要设计对照实验（如部分用户固定权重作为对照组），否则无法判断这项能力是否真正有价值，还是只是增加了成本和不确定性。这是本版最需要在上线前想清楚的方法论问题。
- 大模型供应商选型与合规审查结论（境内部署、数据不用于训练、留存期限），依赖 6.2 的法务评审结果。
- 提示词的版本管理与效果回归方式：提示词变更会影响所有用户的建议和总结质量，需要确定是否建立提示词版本化与灰度机制。
- 是否需要人工审核兜底机制：当安全审核层拦截率或用户"不准"反馈超过阈值时，是否引入人工介入流程。
