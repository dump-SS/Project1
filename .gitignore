node_modules/
dist/
*.tsbuildinfo
.vite/

.env
.env.local
.env.*.local

__pycache__/
*.pyc
.venv/
venv/
backend/data.db
backend/data.db-journal

# 测试专用 SQLite 库与索引目录（tests/conftest.py 自动创建，与开发库隔离）
backend/.pytest_data/

# FAISS 向量索引（27MB+ 生成物，由 scripts/import_knowledge_points.py 重建，不入库）
backend/kb_vectors/

mock-server/data.db

.reasonix/
reasonix.toml

# WorkBuddy 本地记忆与配置(不上库)
.workbuddy/
.workbuddy-ai/

# ZCode 会话本地数据(不上库)
.zcode/

# Trae HTML 分享缓存(不上库)
.trae-html-share-packages/

# 本地工作区数据 / 内网穿透配置
.microengine-deploy/

# 调试与临时文件
*.log
*.txt
# 例外：prompt 模板是产品资产，必须入库。
# `*.txt` 原本用于忽略调试输出，但会连带忽略 backend/prompts/ 下新增的模板
# （suggestion.txt 因早已被跟踪而不受影响，新文件却会被静默忽略——已踩过一次）。
!backend/prompts/*.txt
.neon

# 内部分派文档(不上库，仅供团队内部发活用)
docs/refactor-dispatch-cards.md

# 本地汇报页(暂不入库)
docs/refactor-development-briefing.html

# Agent 规则文件(本机环境专用，内容含本机 git 坑，对他人无用)
AGENTS.md
CLAUDE.md
.cursorrules
.windsurfrules
.cursor/
.github/copilot-instructions.md
