# 学习状态智能助手 PRD v1.5（MVP：第一板块 + 板块二详细设计）

- 文档状态：评审通过（2026-08-29 确认）
- 版本：v1.5
- 日期：2026-08-18（v1.5 修订于 2026-08-29）
- **阶段口径：pilot（小范围试用验证）**
- 覆盖范围：产品整体三大板块概览 + 第一板块 MVP 详细设计（已交付）+ **第二板块「垂直学科落地」详细设计（v1.4 新增）**
- 后续板块（第三板块）本版仅作规划性描述

> **关于阶段口径（v1.5 补充，全文适用）**
>
> 本文档覆盖的是 **pilot 阶段**——小范围试用、验证产品假设，不是 beta，更不是正式对外发布。开发团队为学生团队，没有法务部门，也没有外部评审资源。
>
> 因此，文中凡涉及「法务评审」「正式隐私工程评审」「上线门硬前置」这类按正式发布尺度书写的要求，在 pilot 阶段按下列三档执行：
>
> | 类别 | pilot 阶段怎么做 |
> |---|---|
> | **设计底线**（与阶段无关，必须做） | 监护人授权、撤回即删除、最小群体规模 k 值下限、数据不出境、未成年人相关告知 |
> | **团队内完成即可** | 授权文案由团队自撰，并在页面显著位置标注「学生团队开发，未经专业法律审核」；「正式隐私工程评审」降级为团队内部自查并留书面记录 |
> | **记入待办，不阻塞进度** | 正式法律意见、外部隐私评审签字、差分隐私、CI 断言自动化 |
>
> 本文所有「上线」均指 **pilot 上线**。正式对外发布前，须重新评估第三类降级项。

变更记录：
- v1.5：新增「阶段口径」声明，明确本文档覆盖 pilot 阶段。文中按正式发布尺度书写的合规要求（法务评审、正式隐私工程评审、上线门硬前置）统一按 pilot 重新分级为「设计底线 / 团队内完成 / 记入待办」三档，并在相关条目处就地标注。**产品需求本身无变更。**
- v1.4：新增板块二「垂直学科落地」详细设计。明确本地知识库选型（推荐 RAG 折中方案）、知识内容维度（知识点 / 错题 / 概念图）的数据模型与 API、板块二与板块一 5.4 复盘的边界、阶段化实施计划（v2.1 / v2.2 / v2.3）、数据出域统一决策。同步在「整体三板块状态」中将板块二从「规划中」升级为「本版详细设计」。
- v1.3：版本号统一调整至 v1.3（内容同 v1.1，无新增变更）。
- v1.1：确定接入第三方大模型 API。新增第 6 节「AI 能力架构与大模型接入」；5.2 状态量化由纯规则改为「规则计算 + AI 动态调权（受限区间）」；5.3 建议生成由规则模板改为「LLM 生成 + 安全审核层」；新增 5.4「学习总结与复盘」；相应更新数据模型、隐私与非功能需求、指标与开放问题。原第 6-10 节顺延为第 7-11 节。新增第 0 节「与市面现有产品的差异与优势」，前置阐述差异化定位。
- v1.0：初版，三板块概览 + 板块一 MVP 详细设计（纯规则算法方案）。

---

## 0. 与市面现有产品的差异与优势

本节用于对外沟通与对内定位，说明本产品为什么不与现有工具直接竞争、而是占据一个目前空白的位置。所列市面产品特征基于公开信息的概括，具体竞品的功能与定价以各自官网为准，建议在对外使用前做一轮核实。

### 0.1 市面现有产品的类型与盲区

目前面向基础教育学习场景的产品大致分为四类，各自解决了部分问题，但都没有覆盖"学习状态"这个维度：

| 类型 | 代表形态 | 解决的问题 | 盲区 |
|---|---|---|---|
| 题库/拍搜类 | 拍照搜题、智能题库、刷题 App | 找到题、对答案、看解析 | 只关注"题做对了没有"，不关注"这个学生现在状态适不适合继续学" |
| 课程/辅导类 | 网课、1 对 1、AI 课 | 内容讲解与知识传递 | 内容是核心资产，学生的情绪、疲劳、节奏不在其闭环内 |
| 时间管理/计划类 | 番茄钟、待办清单、学习计划 App | 安排时间、打卡提醒 | 按时间表执行，不管执行时的实际状态，计划不会随状态自适应调整 |
| 通用 AI 助手 | 大模型对话类产品 | 答疑、讲解、通用学习建议 | 无长期学习数据积累，每次对话从零开始，建议无上下文、无连续性 |

**共同盲区**：这四类都以"内容"或"时间"为中心，没有一类以"学习者当下的状态"为中心。而状态（专注度、疲劳、情绪的动态变化）恰恰是影响学习效率最直接的变量，也是家长和学生在分数之外最缺乏客观感知的部分。

### 0.2 本产品的差异化定位

本产品的切入点不是再做一个题库或课程，而是做**学习状态的感知与规划中枢**，与上述产品形成互补而非替代：

- **状态量化是核心资产，不是附加功能**。市面产品的"学习报告"多为事后统计（做了多少题、正确率多少），本产品的状态分是行为数据+自评的动态量化，并且随时间形成趋势，用于**事前**调整规划，而非仅用于事后展示。
- **规划随状态自适应，而非固定模板**。计划类产品的时间表是静态的，本产品的导学规划会根据最近一次状态评估动态调整任务强度——状态差时降量保节奏，状态好时适度加量，这是"以状态为中心"与"以时间为中心"的本质区别。
- **AI 负责表达与适配，规则负责事实与底线**。相比通用 AI 助手"每次从零开始"，本产品把模型调用限定在有长期数据积累的上下文里（历史状态、权重、目标进展），建议和复盘有连续性；同时状态分等关键事实由确定性规则计算，不交给模型，避免了通用 AI 在数字和判断上的不可靠。

### 0.3 三板块组合的纵深

单一功能容易被模仿，本产品的长期优势在于三板块组合形成的数据与能力纵深，这是市面上单一类型产品难以复制的：

- **板块一（本版）**建立状态感知能力与用户信任，是入口。
- **板块二**把状态与具体学科内容打通（接入本地知识库、长期学习画像），形成"既懂你的状态、又懂你学的内容"的组合——市面上做内容的不懂状态，做状态（若有）的不懂内容。
- **板块三**在合规前提下做匿名群体参照，让个体的状态评估有了"同龄人/同目标群体"的横向坐标，进一步加深数据壁垒。

**需要坦诚的一点**：上述优势是定位层面的判断，不是已验证的事实。状态量化是否真比"凭感觉"更能帮到学生、AI 调权是否真比固定权重更准，都需要 MVP 上线后用数据验证（见第 9 节指标与第 11 节开放问题）。本节的对外表述应避免过度承诺，尤其在涉及"提升成绩"类效果宣称时，需遵守广告法与教育类产品的宣传规范。

---

## 1. 背景与问题定义

基础教育阶段（尤其中学）学生普遍存在以下问题：

- 学习前缺乏系统规划，凭感觉安排时间和内容，效率低。
- 学习状态（专注度、疲劳度、情绪、掌握程度的变化趋势）缺乏客观感知，学生和家长往往只能凭"感觉"或考试分数事后判断，反馈滞后。
- 学习建议普遍是通用型内容（如"多刷题""该复习了"），没有结合个人实际学习内容、进度和当下状态。

产品目标：用 AI 把"学习规划 - 状态感知 - 个性化建议"串成一个闭环，让学生学习前有方向、学习中有反馈、学习后有调整依据。

## 2. 产品整体架构（三大板块）

产品分三大板块，逐步建设，本 PRD 仅对板块一做完整设计，板块二、三做方向性说明以确保架构不冲突。

| 板块 | 定位 | 核心能力 | 本版状态 |
|---|---|---|---|
| 板块一 | 学习状态与规划中枢（面向基础教育） | 导学规划、状态动态量化（AI 调权）、个性化学习建议、学习总结复盘 | **MVP，v1.0-v1.3 已交付** |
| 板块二 | 垂直学科落地场景 | 学科知识库接入、知识点掌握画像、错题归因与归纳、概念关联图谱、长期学习画像 | **v1.4 详细设计，下个里程碑实施**（见第 12 节） |
| 板块三 | 群体参照 | 匿名特征值上传，跨用户群体状态/方法对比参照，不上传原始学习内容 | 规划中，方向性描述 |

三个板块的数据关系：板块一产生的"状态量化结果"和"学习画像片段"是板块二长期用户画像的基础输入之一；板块三只消费板块一/二产出的**匿名化统计特征**，不接触原始内容，三者在数据边界上要在架构上提前预留隔离层（见 10.3）。

高等教育阶段的板块一延伸暂不在本版规划，待中学阶段模型验证后再设计（高等教育学习自主性、学科专业化程度更高，评估维度和导学逻辑需要单独调研，不能直接套用中学模型）。

## 3. MVP 范围定义

- **覆盖学段**：初中、高中（不含小学、不含高等教育）。理由：中学阶段学习自主性和目标明确性介于小学和大学之间，行为数据+自评的组合方案适配度最高，且是 K12 辅导市场主流需求。
- **产品形态**：Web + 小程序，不做原生 App。理由：MVP 阶段优先验证算法和体验假设，Web/小程序开发和分发成本低，可快速迭代；原生 App（含日常打卡、状态感知的系统级能力）留作后续版本规划。
- **MVP 只做板块一**，包含四个功能模块：
  1. 学习前导学规划
  2. 学习状态动态量化评估（含 AI 动态调权）
  3. 个性化学习建议（LLM 生成）
  4. 学习总结与复盘（LLM 生成，状态与规划维度）
- **AI 接入方式**：调用第三方云端大模型 API，不自行训练或微调模型。模型负责表达与判断倾向，不负责计算事实，详见第 6 节。

不在 MVP 范围内：本地知识库接入、学科知识内容复盘（板块二）、群体匿名对比（板块三）、高等教育适配、模型自训练/微调。

## 4. 用户与场景

### 4.1 目标用户

- 主用户：初中生、高中生（13-18 岁），自主使用产品进行日常学习管理。
- 关键相关方：家长（可能有查看/监督需求，但本版不做家长端，仅预留数据接口可能性）、学校/教师（暂不作为 MVP 用户角色）。

因用户群体为未成年人，产品在数据采集、隐私保护、内容安全上需要更严格的默认设置，具体见第 8.1 节（隐私与未成年人保护）和 6.2（AI 数据出域边界）。

### 4.2 典型场景

1. 小凡（高二）晚自习前打开产品，希望知道今晚该先复习什么、花多久——触发"导学规划"。
2. 小凡做完一轮数学练习后，手动录入本次练习的正确率和用时，产品结合页面失焦次数等行为数据，以及他自己勾选的"有点烦躁/一般专注"自评，更新他的学习状态，并在后续规划里做调整——触发"状态动态量化"。
3. 系统发现小凡最近三天英语单词记忆正确率持续下降且自评疲劳度上升，主动提示调整复习节奏或建议短时高频而非长时低效——触发"个性化建议"。

## 5. 功能需求详细设计

### 5.1 学习前导学规划

**目标**：学生打开产品时，能在 1-2 分钟内获得一份"今天/本阶段该学什么、怎么学、学多久"的可执行规划，而不是空白页面发呆。

**输入**：
- 学段、年级、学科（用户设置，一次性）
- 短期目标（如"两周后期中考试""本周弄懂函数章节"）与长期目标（如"高考目标分数区间"），用户手动输入或从预设模板选择
- 当前可用学习时间（用户输入，如"今晚 2 小时"）
- 历史学习数据（若有）：最近的状态量化结果、已完成任务、薄弱知识点标记

**处理逻辑（MVP 简化版，非板块二的知识库深度诊断）**：
1. 若无历史数据（新用户）：基于用户填写的目标和年级学科，套用规则模板生成初版计划（如"目标是期中考数学 120+，本周安排：函数专项 3 次、综合练习 2 次"）。
2. 若有历史数据：结合最近一次状态量化结果（见 5.2）动态调整任务量和难度——状态偏疲劳/低效时降低任务强度、延长单次任务间隔；状态偏高效稳定时可适度提高强度或引入拔高内容。
3. 输出结构化计划：任务列表（学科、内容方向、预计时长、优先级），非具体题目内容（题目内容归属板块二知识库能力，MVP 不做）。

**交互要点**：
- 用户可对生成的计划进行手动调整（改时长、删减任务），调整行为本身也作为后续算法的反馈信号记录。
- 计划完成情况需要用户在学习后简单确认（完成/部分完成/未完成），作为下一次规划和状态量化的输入。

**MVP 边界**：不做具体知识点内容推送、不做题库、不做智能出题，只做"任务级"规划，内容颗粒度到"学科+方向+时长"。

### 5.2 学习状态动态量化算法

**目标**：把学生一次学习活动或一段时间（如一天/一周）的状态变化，转化为可比较、可追踪的量化指标，作为规划调整和建议生成的依据，而不是黑箱评分。

**数据来源（行为数据 + 自我报告结合）**：

| 类型 | 数据项 | 采集方式 |
|---|---|---|
| 行为数据 | 学习时长、任务完成度（完成/部分/放弃）、练习正确率（若有客观测验/打卡）、学习节奏（是否按计划时段学习、中断次数、切出应用次数——小程序场景下可采集页面失焦次数作为弱信号） | 系统自动记录，用户无感 |
| 自我报告 | 每次学习结束后的轻量自评：专注度（1-5）、疲劳度（1-5）、情绪状态（正向/中性/负向的简单三选）、对本次内容难度的主观感受（偏易/适中/偏难） | 用户主动填写，控制在 10 秒内完成，避免造成填报负担 |

**算法设计（规则计算为主体 + AI 动态调权，分数计算过程本身保持确定性可复现）**：

1. **单次学习状态分**：对行为数据和自评数据分别标准化（0-1），按当前权重加权得到单次状态分。计算公式固定，权重可由 AI 调整（见第 4 点）。
   - 行为子分 = w1×完成度 + w2×正确率（若无正确率数据则该项权重归零并重新分配） + w3×节奏稳定度（中断次数的反向标准化）
   - 自评子分 = w4×专注度 + w5×(6-疲劳度) + w6×情绪正向程度
   - 单次状态分 = α×行为子分 + β×自评子分（初始 α=β=0.5，各 w 初始等权；所有权重存于后台配置与用户级权重表，不写死在代码逻辑里）
2. **趋势量化**：以滑动窗口（如最近 7 次学习记录或最近 7 天）计算状态分的均值和斜率（简单线性回归斜率即可，不需要复杂时序模型），得到"当前状态水平"和"变化趋势（上升/持平/下降）"两个维度。
3. **状态标签化**（供用户直观理解，避免只给一个抽象数字）：将（水平，趋势）二维结果映射为几类状态标签，例如：
   - 高效稳定：水平高 + 趋势平或升
   - 疲劳预警：水平中低 + 疲劳自评持续偏高 + 趋势降
   - 情绪受阻：情绪自评连续偏负 + 正确率/完成度下降
   - 波动上升期：水平中等但趋势明显上升
   - 数据不足：记录次数低于最小阈值（如 3 次）时不下结论，仅展示原始记录
   - 注：上述标签判定中"水平高/中低""趋势明显上升"等词对应的具体数值区间（如状态分阈值、回归斜率阈值），待算法实现阶段基于初始权重和真实数据分布校准确定，本 PRD 不预设具体数值，避免无数据依据的凭空设定。
4. **AI 动态调权（本版核心能力）**：由大模型分析用户历史数据，输出权重调整建议，但调整幅度受硬限制区间约束，模型不能直接改写最终权重。
   - **触发时机**：按周期离线批量执行（如每周一次或累计满 N 条新记录时），不在用户单次学习后实时调权——避免单次异常数据导致权重抖动，也便于控制 API 成本。
   - **输入**：该用户近期的结构化特征序列（各维度标准化值、状态分、趋势、计划完成情况、用户对建议的反馈），以及当前权重值。
   - **输出**：各权重的建议值 + 调整理由（理由必须返回，用于 7.3 可解释性和问题回溯）。
   - **硬限制（必须在代码层强制校验，不依赖提示词约束）**：
     - 每个权重有允许区间，如 α、β ∈ [0.3, 0.7]，各子项权重 ∈ [0.1, 0.5]，且同组权重之和归一化为 1。
     - 单次调整幅度上限（如任一权重单次变动不超过 0.1），防止突变造成用户状态分跳变。
     - 模型返回值越界、格式非法、字段缺失或 API 调用失败时，**一律回退到当前权重**（不是回退到初始值，避免丢失已积累的校准结果），并记录异常日志。
   - **每次调权需留痕**：记录调整前后权重、模型给出的理由、生效时间，支持回溯与人工回滚。
   - **用户可见性**：不向用户暴露权重数值和公式（延续 5.2 输出原则），但用户可在设置中关闭"AI 自动调整"，改用固定默认权重，保留用户的最终控制权。
   - **不做**：不做跨用户的联合训练或模型微调（涉及未成年人数据，MVP 阶段不触碰）；权重调整仅在单用户维度内进行。

**输出**：
- 给用户的呈现：状态标签 + 简要说明（不展示原始权重公式，避免用户过度纠结"分数"本身，弱化应试化倾向）
- 给内部系统的呈现：结构化状态分和趋势数据，作为 5.1 规划调整和 5.3 建议生成的输入参数

**冷启动处理**：新用户前几次记录数据不足，状态量化模块不强行输出结论，仅提示"数据积累中"，避免早期误判影响体验。

**异常与边界场景**（具体处理策略由实现阶段定义，此处仅列出需覆盖的场景避免遗漏）：
- 数据断层：用户连续多天（如超过滑动窗口长度）未产生学习记录，窗口内数据过旧时如何处理——是否重置趋势计算、是否提示"重新开始记录"。
- 记录删除回溯：用户手动删除部分学习记录后，已生成的状态评估是否回溯重算，还是仅影响后续计算。
- 极端自评值：用户连续多次全部选择极端值（如疲劳度全部满分）时，是否触发异常数据标记而非直接纳入计算。

**MVP 不做**：不做跨学科的加权综合（不同学科状态先分开评估，避免语文情绪和数学正确率强行合并成一个分数导致语义混乱）；不自行训练或微调模型（仅调用第三方 API）；不让模型直接产出状态分本身（分数始终由固定公式计算，模型只影响权重）。

### 5.3 个性化学习建议

**目标**：结合 5.1 的计划执行情况和 5.2 的状态量化结果，以及用户实际学习的学科/内容方向，给出具体、可执行、非通用模板的建议。

**触发时机**：
- 单次学习结束后（即时反馈，1-2 条简短建议）
- 阶段性回顾（如每周一次，汇总本周状态趋势给出调整建议）

**建议生成逻辑（LLM 生成 + 安全审核层，规则兜底）**：

1. **结构化上下文组装**：由规则层先算出确定性结论（状态标签、趋势、计划完成情况、薄弱学科方向），组装成结构化上下文，而不是把原始数据一股脑丢给模型。
2. **LLM 生成建议**：模型基于上下文生成具体、口语化、贴合该学生实际学习内容的建议，解决 v1.0 规则模板"话术固定、久用即腻"的问题。
3. **安全审核层过滤（强制，不可绕过）**：生成内容展示给用户前必须过审，详见 6.3。
4. **规则兜底**：API 超时/失败/审核不通过时，回退到按"状态标签 + 学科 + 计划执行情况"匹配的建议模板库，保证用户始终能拿到可用建议，不出现空白。兜底模板库仍需维护，示例：
   - 状态=疲劳预警 + 学科=数学 + 最近正确率下降 → 建议缩短单次时长、增加间隔、暂缓新内容转为已掌握内容的巩固练习
   - 状态=情绪受阻 + 计划连续两次未完成 → 建议主动降低下一次任务量，并提示"降低目标不等于放弃目标"，避免打击积极性
   - 状态=高效稳定 + 趋势上升 → 建议适度增加拔高内容或提前进入下一阶段目标

**边界与安全**：
- 建议内容不涉及心理诊断或医疗建议，涉及情绪相关表述使用鼓励、中性、非评判性语言（如不说"你很焦虑"，而说"最近感觉有点吃力，我们把节奏放慢一点"）。
- 若自评数据连续显示强烈负向情绪信号（如连续多次选择负向情绪且伴随疲劳度满分），MVP 阶段不做心理干预建议，仅温和提示"如果最近学习压力比较大，也可以和老师或家长聊一聊"，不越界扮演心理咨询角色。
- LLM 生成的建议同样受上述两条约束，通过提示词约定 + 安全审核层双重保证（提示词只是第一道，不能作为唯一防线）。

### 5.4 学习总结与复盘（LLM 驱动）

**目标**：把一段时间内零散的学习记录和状态变化，整理成学生看得懂、用得上的复盘内容，而不是让用户自己看一堆数据图表。

**范围说明**：本节是板块一内的"状态与规划维度"总结（我学得怎么样、节奏对不对、下阶段怎么调）。板块二的"学科知识内容归纳复盘"（具体知识点掌握、错题归因）不在本版范围，两者边界见 10.1。

**触发方式**：
- 周期性自动生成：每周一次周复盘，学习记录数达标才生成（不足则提示数据不够）。
- 用户主动触发：用户可手动要求生成某个时间段的总结，需做频率限制（如每日上限若干次）以控制 API 成本。

**输入**：该周期内的学习记录、状态分与趋势、状态标签变化、计划完成情况、目标进展，以及用户填写的目标描述文本。

**输出内容结构**（固定框架，避免模型自由发散导致每周格式不一）：
- 这段时间的状态概览（用自然语言解释状态标签变化，而非只给分数）
- 观察到的规律（如"数学安排在晚上时完成度明显低于下午"）
- 下阶段建议（1-3 条，可直接落到 5.1 的规划调整）
- 一句鼓励性收尾（语气约束同 5.3）

**质量与安全约束**：
- 总结必须基于真实数据，禁止编造未发生的学习行为或不存在的进步（幻觉风险是本功能最主要的质量风险）。实现上采取：仅传入结构化数据 + 要求模型引用具体数据点 + 审核层校验关键数字与数据库是否一致。
- 数据不足时不硬凑总结，明确告知"本周记录较少，暂不生成完整复盘"。
- 生成内容同样经 6.3 安全审核层。

**用户反馈**：总结页提供"准不准/有没有用"的轻量反馈入口，作为后续优化提示词和调权策略的依据。

## 6. AI 能力架构与大模型接入

本节统一定义大模型的接入方式、数据边界和安全机制，5.2、5.3、5.4 三处 AI 能力均遵循此约定。

### 6.1 能力分工

| 能力点 | 模型职责 | 确定性部分（不交给模型） | 失败回退 |
|---|---|---|---|
| 状态量化调权（5.2） | 分析历史特征，建议权重值 + 理由 | 状态分公式计算、权重区间校验、标签判定 | 保持当前权重 |
| 个性化建议（5.3） | 生成贴合个人情况的建议文案 | 状态标签与触发条件的判定 | 规则模板库 |
| 学习总结复盘（5.4） | 组织自然语言复盘、总结规律 | 数据统计、趋势计算、数字校验 | 提示稍后再试，不展示半成品 |

原则：**模型负责表达和判断倾向，不负责计算事实**。所有对用户可见的数字和结论性标签都来自规则层，模型不得覆盖。

### 6.2 数据出域边界（重要）

MVP 采用第三方云端大模型 API，涉及数据出域，需明确边界：

**允许传输**：
- 结构化脱敏特征（各维度标准化值、状态分、趋势、标签、计划完成情况）
- 用户自行填写的文本内容（学习目标描述、任务备注等）——经产品决策允许传输，以提升建议与总结的贴合度

**禁止传输**：
- 用户身份信息：真实姓名、手机号、监护人联系方式、学校/班级、账号 ID（如需关联请用随机化的临时会话标识，且不可反查）
- 精确地理位置、设备指纹类信息

**因允许用户文本出域而必须落实的义务（不可省略）**：
- 隐私政策与首次使用告知中**明确写出**："你填写的学习目标等文字内容会发送给第三方 AI 服务用于生成建议"，不能只笼统写"使用第三方服务"。
- 监护人授权环节需覆盖此项数据处理，作为授权内容的一部分明示。
- 输入框旁给出轻量提示，引导学生不要填写个人隐私信息（如家庭住址、联系方式）。
- 选择 API 供应商时确认合同条款：不得用于模型训练、数据留存期限尽可能短、有中国境内合规部署方案（未成年人数据跨境传输的合规风险显著，若供应商无境内节点则不予采用）。
- 对用户输入做敏感信息检测（如手机号、身份证号模式匹配），命中时在发送前脱敏或阻断。
- 提供开关：用户/监护人可选择"不发送我的文字内容"，此时降级为仅用结构化特征生成，功能可用性略降但不阻断。

**已知风险**：允许未成年人填写的自由文本出域，即便有上述措施，仍存在学生无意填入敏感信息并被第三方留存的风险。这是本版本合规风险最高的一项。

**pilot 阶段怎么处理**：不做外部法务评审，改为团队内部评估并留书面记录。若判断风险不可接受，直接关闭自由文本通道（该能力已有开关，关闭不影响其余功能）。**正式对外发布前须补法律意见**（见文首阶段口径第三类）。

### 6.3 内容安全审核层

所有展示给用户的模型生成内容（5.3 建议、5.4 总结）必须经过审核，审核不通过则回退或不展示：

- **禁止内容**：心理诊断或病理化表述、贬低打击性评价、与学习无关的话题延展、医疗/用药建议、任何鼓励过度学习损害健康的表述（如"再熬一晚就好"）、涉及自我伤害的内容。
- **强制语气**：鼓励、中性、非评判，符合未成年人沟通规范。
- **实现方式**：供应商侧内容安全能力 + 自建关键词/正则黑名单 + 关键数字与数据库一致性校验（针对 5.4 幻觉）三层结合，不依赖单一手段。
- **危机信号识别**：若用户文本或自评数据出现自我伤害等强风险信号，不由模型自由回应，直接切换为固定的、经审定的引导文案（建议联系老师、家长或专业帮助渠道），并记录事件供人工关注。此路径必须硬编码，不走 LLM 生成。
- **人工抽检**：上线初期对生成内容做人工抽样审查，作为提示词与黑名单的迭代依据。

### 6.4 成本、性能与可用性

- **调用时机设计以控成本为前提**：调权按周期批量执行，总结按周生成，建议在单次学习后生成（频次最高，需评估成本，可考虑对低活跃用户降频）。
- **异步化**：所有模型调用异步执行，不阻塞用户主流程（用户不必等生成完才能看计划）；生成完成后以消息/角标提示。
- **超时与重试**：设定超时上限（如 10 秒），失败重试不超过 2 次，仍失败即走回退路径。
- **供应商抽象层**：接入层需对模型供应商做接口抽象，避免与单一厂商强耦合，便于因合规、成本或稳定性原因切换。
- **成本监控**：需有按用户/按功能的调用量与成本统计，并设置总量告警阈值，防止异常调用导致费用失控。

### 6.5 AI 输出的可解释性与留痕

- 每条建议、总结、权重调整都需存储其输入依据和模型返回的理由，用户侧可查看"依据了哪些数据"的简版说明。
- 用户可反馈"这条不准"，反馈与当次输入上下文一并留存，作为提示词与调权迭代的输入。

## 7. 信息架构与数据模型（简要）

核心实体（MVP 范围）：

- `User`：学段、年级、学科列表、目标设定（短期/长期）
- `LearningPlan`：计划所属用户、生成时间、任务列表（学科、方向、预计时长、优先级、状态：待完成/完成/部分完成/放弃）
- `LearningRecord`：单次学习行为数据（时长、完成度、正确率(可空)、中断次数）+ 自评数据（专注度、疲劳度、情绪、难度感知）+ 关联的计划任务(可空，允许自由学习不挂靠计划)
- `StateAssessment`：基于一批 `LearningRecord` 计算得到的状态分、趋势、标签，按学科分别存储
- `Recommendation`：生成的建议内容、触发依据（关联的 StateAssessment 和 LearningPlan）、生成方式（LLM 生成/模板兜底）、用户反馈（有用/一般/没用）
- `UserWeightConfig`：用户级权重表（α、β 及各子项权重当前值）、是否启用 AI 自动调权、最近更新时间
- `WeightAdjustLog`：每次调权留痕——调整前后权重、模型返回的理由、是否发生越界回退、生效时间，支持回溯与人工回滚
- `LearningSummary`：周期性复盘内容、覆盖时间范围、引用的数据点、生成状态（成功/数据不足/生成失败）、用户反馈
- `AICallLog`：模型调用记录——功能类型（调权/建议/总结）、输入摘要、耗时、是否成功、审核结果、成本相关计量，用于 6.4 成本监控和问题排查

数据边界说明：本版数据模型只在用户自己账号下闭环，不涉及匿名化上传（板块三）和本地知识库调用（板块二），为后续板块预留字段扩展空间但不实现。涉及大模型调用的字段需按 6.2 的出域边界设计，`AICallLog` 中不得存储完整的用户身份信息。

## 8. 非功能需求

### 8.1 隐私与未成年人保护（高优先级）

- 目标用户为未成年人，需遵循《未成年人网络保护条例》及《个人信息保护法》相关要求：最小必要原则采集数据、监护人授权机制、提供数据删除/导出入口。涉及第三方 AI 服务的数据处理需单独告知并取得授权，详见 6.2。
- 自评中的情绪、疲劳等数据属于敏感度较高的行为画像数据，需加密存储，明确告知用途，不用于本版之外的商业化画像用途。
- 需要家长/监护人授权流程，MVP 阶段采用简化的监护人邮箱/手机号确认机制，具体规则：
  - 首次使用时需填写监护人联系方式并发送确认请求，监护人点击确认后授权生效。
  - 授权有效期 30 天，到期后需重新确认；期间监护人可通过确认链接中的入口随时撤销授权，撤销后账号进入只读状态（可查看历史数据，不可新增记录），直至重新授权。
  - MVP 阶段无法核验监护人与用户的真实关系（如用户填写自己的邮箱冒充监护人），作为已知风险记录，依赖后续版本引入更强的身份核验手段（如实名认证），本版以流程存在为合规底线。

### 8.2 性能与体验

- 自评填报流程需控制在 10 秒内完成，避免打断学习节奏。
- 状态量化计算可异步进行，不阻塞用户查看学习计划的主流程。
- 大模型调用全部异步，超时与回退策略见 6.4；任何 AI 功能不可用时，产品核心流程（记录学习、查看计划）必须仍然可用。

### 8.3 可解释性

- 状态标签和建议都需要能追溯到"为什么"（对应哪些行为数据/自评数据），避免用户觉得是黑箱评分，产生不信任或抵触情绪。
- 引入 AI 调权后，可解释性要求不降低：权重调整需留痕并可回滚，具体见 6.5。

## 9. 关键指标与验收标准

MVP 上线后重点观察（首版上线以观察趋势为主，不设硬性阈值，待积累 2-4 周真实数据后再确定各指标基线和目标值，避免团队对"什么算成功"产生分歧）：

- 导学规划：计划生成后的采纳率（用户是否直接使用/仅小幅调整 vs 大幅修改或忽略）
- 状态量化：自评填报完成率（体现负担是否可控）、状态标签与用户主观认可度的一致性（可通过简单反馈"这个判断准不准"收集）
- 个性化建议：建议的"有用"反馈占比、建议后的行为改变率（如收到"降低任务量"建议后下一次任务量是否确实调整）
- 留存：连续 7 日、14 日使用率，作为闭环是否形成习惯的核心信号
- AI 能力质量：建议/总结的"有用"与"不准"反馈比例、安全审核层拦截率（拦截率异常高说明提示词有问题）、生成失败与回退触发率
- AI 成本效率：单用户月均 API 成本、各功能调用占比，用于判断商业模型是否成立

验收标准（MVP 可上线的最低要求）：
- 四个模块功能可用，冷启动（新用户前 3 次使用）体验不出现明显断裂或报错
- 状态量化在数据不足时不强行输出误导性结论
- 未成年人数据保护基础机制（监护人授权、数据可删除）落地，不作为后续版本再补
- AI 相关硬性要求：
  - 权重调整越界时确实回退且有日志，不出现状态分跳变
  - 大模型 API 全部不可用时，建议走模板兜底、核心流程不受影响
  - 安全审核层对已知禁止内容（心理诊断、贬低性表述、危机信号）有明确拦截用例并通过测试
  - 6.2 涉及的隐私告知与监护人授权文案已随功能上线，且页面已标注「学生团队开发，未经专业法律审核」<br>**〔pilot 口径〕**团队自审替代法务评审——文案本身仍是设计底线，不可省略；外部法律意见记入待办，正式发布前补

## 10. 后续板块规划方向（非本版交付，仅做架构预留说明）

### 10.1 板块二：垂直学科落地场景

- 按场景/学科/专业提供知识内容归纳与复盘能力，接入本地知识库。"本地知识库"的候选技术形态（待板块二立项时评审确定，本 PRD 仅列出可能方向以支撑架构预留判断）：
  - 本地部署的开源 LLM + 私有学科语料微调（隐私最强，算力成本高）
  - 云端通用 LLM + 本地向量数据库做检索增强（RAG），原始内容不出本地（折中方案）
  - 纯本地文件索引 + 规则化知识归纳（无生成式模型，能力受限但实现最简单）
  - 上述形态对数据是否出域、模型推理位置的要求差异很大，直接影响板块一数据模型需要预留的接口形态，因此即便板块二不实现，也建议在 MVP 数据层设计时按"原始内容默认不出本地"的最严格假设做预留。
- 承载用户长期学习画像：整体规划、短长期目标进展会被此处模型调用，即板块一产出的 `StateAssessment`、`LearningPlan`、`LearningSummary` 历史数据是板块二画像的输入源之一。
- 需要单独评估：本地知识库的数据来源、更新机制、不同学科知识结构的差异化建模。
- **与 5.4 的边界**：板块一的 5.4 只做"状态与规划维度"的复盘（学习节奏、状态趋势、计划执行）；板块二做"知识内容维度"的复盘（知识点掌握、错题归因、概念关联）。前者输入是行为与状态数据，后者输入是学习内容本身，需要知识库支撑。实现时注意两者的总结入口在用户侧应有清晰区分，避免用户困惑于"为什么有两个复盘"。
- **模型部署形态的衔接问题**：板块一 MVP 采用云端 API 且允许用户文本出域，板块二若采用本地知识库/本地模型（出于隐私考虑），两者的数据处理策略会不一致。需要在板块二立项时统一决策：是把板块一也迁到本地/私有化部署，还是明确区分"哪类数据走云端、哪类只留本地"。这个问题越晚决策，改造成本越高。

### 10.2 板块三：群体匿名参照

- 只上传匿名特征值（如状态标签分布、某类学习方法的效果统计），不上传任何原始研究/学习内容。
- 需要设计匿名化/脱敏规则和最小群体规模限制（避免小样本群体反推出个体身份），这是该板块能否成立的前置条件，不能等做完了再补。<br>**〔pilot 口径〕**匿名化规则与 k 值下限属设计底线，必须完成；「专门的隐私工程评审」在 pilot 阶段降级为团队内部自查并留书面记录，外部评审签字记入待办。

### 10.3 架构预留建议

即便 MVP 不实现板块二、三，建议在数据层设计上：
- 板块一产出的数据默认不跨用户可见，任何未来的"匿名聚合"都应是显式的、用户可感知的授权动作，不能是默认行为。
- 数据模型中为"匿名特征提取"预留独立的统计视图层，避免后续为了做板块三去改板块一的原始数据结构。
- 大模型接入层做供应商抽象（见 6.4），为后续可能的本地化/私有化部署留出替换空间。

## 11. 开放问题（需要产品后续决策，本版不下结论）

- 高等教育阶段的板块一延伸方案，需要单独调研（自主学习场景、评估维度都可能与中学差异较大）。
- 家长端角色是否在板块一之后单独立项，还是作为板块一的子功能，需要看 MVP 数据反馈和用户诉求。
- AI 调权的效果验证方式：如何证明"AI 调权后的状态评估比固定权重更准"？需要设计对照实验（如部分用户固定权重作为对照组），否则无法判断这项能力是否真正有价值，还是只是增加了成本和不确定性。这是本版最需要在上线前想清楚的方法论问题。
- 大模型供应商选型与合规审查结论（境内部署、数据不用于训练、留存期限），依赖 6.2 的审查结果。<br>**〔pilot 口径〕**供应商合同条款的三项核查（境内部署、数据不用于训练、留存期限）属设计底线，必须逐条核对并留记录；「法务评审」降级为团队内部审查，正式对外发布前补法律意见。
- 提示词的版本管理与效果回归方式：提示词变更会影响所有用户的建议和总结质量，需要确定是否建立提示词版本化与灰度机制。
- 是否需要人工审核兜底机制：当安全审核层拦截率或用户"不准"反馈超过阈值时，是否引入人工介入流程。

---

## 12. 板块二：垂直学科落地场景（详细设计 · v1.4 新增）

### 12.1 板块目标与边界

**目标**：在板块一「状态与规划维度」基础上，把视角从"学得怎么样、节奏对不对"延伸到"**学的内容到底掌握得怎么样**"——具体包括：知识点的掌握画像、错题归因、概念之间的关联结构、长期学习画像。补齐板块一留下的"内容侧"空白，让产品从"通用学习管家"变成"懂状态也懂内容"的双重引擎。

**与板块一 5.4 的边界（再次强调，板块二立项时务必让两个入口在 UI 上可区分）**：

| 维度 | 板块一 5.4 复盘 | 板块二知识内容复盘 |
|---|---|---|
| 输入 | 学习行为 + 自评 + 计划执行情况 | 学习内容本身（题、知识点、做题过程） |
| 处理 | 状态量化引擎（规则） + LLM 表达 | 知识库检索 + 错题模式识别 + LLM 表达 |
| 输出 | "你这周节奏偏疲劳" | "函数单调性掌握度 65%，错题集中在复合函数判定步骤" |
| 频次 | 周一次 / 用户手动 | 用户手动 / 学科级 |
| 摘要字段 | `dimension: "state_and_plan"` | `dimension: "knowledge"` |
| 数据出域 | 允许出域（板块一规则） | **默认不出本地**（v1.4 新规则，详见 12.6） |

### 12.2 知识库选型决策

#### 12.2.1 候选方案回顾

| 方案 | 隐私 | 实现成本 | 算力成本 | 能力上限 |
|---|---|---|---|---|
| A. 本地部署开源 LLM + 私有学科语料微调 | 强 | 高 | 高 | 高 |
| B. 云端通用 LLM + 本地向量库做 RAG | 中（原始内容不出本地，仅检索片段 + 用户问题出域） | 中 | 低 | 中-高 |
| C. 纯本地文件索引 + 规则化归纳 | 强 | 低 | 极低 | 低 |

#### 12.2.2 推荐方案：B（RAG 折中方案）

**理由**：
1. **隐私可控**：原始题目内容、错题原文默认**只存本地向量库**，LLM 只能看到检索出来的若干相关片段 + 用户问题描述，无法反向重建用户的全部学习内容；
2. **能力够用**：RAG 在"已知知识库内"的归纳、对比、关联上能力已足够覆盖 80% 学生场景，短板（"知识库外"的推理）暂不构成中学 MVP 的核心需求；
3. **可演进**：方案 A 后期可平替——把云端 LLM 换成本地开源 LLM，向量库不动；方案 C 可作为降级兜底（云端 API 不可用时）。

**否决方案 A 的具体原因**：
- 私有学科语料微调在中学全学科范围内（数语英 + 物化生 + 史地政）成本极高，MVP 阶段 ROI 不成立；
- 训练/微调涉及未成年人数据二次使用，**与 6.2 出域边界冲突**。

**否决方案 C 的具体原因**：
- 纯规则化归纳的"自然语言复盘"几乎不可用，无法满足学生"看得懂、用得上"的需求（5.4 的标准）；
- 与板块一 5.3 / 5.4 的 LLM 链路脱节，UX 不一致。

#### 12.2.3 方案 B 的技术形态

```
┌─────────────────────────────────────────────────────────────────┐
│  学生端（Web / 小程序）                                           │
│  学科页 / 错题本 / 知识图谱 / 知识复盘                            │
└────────────────────────┬────────────────────────────────────────┘
                         │ HTTPS (TLS 1.3)
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  FastAPI 后端（沿用板块一）                                       │
│  /api/v1/knowledge-points · /error-book · /concept-graph         │
│  /api/v1/knowledge-summary                                      │
└────┬───────────────────────────┬────────────────────────────────┘
     │                           │
     ▼                           ▼
┌─────────────┐         ┌──────────────────────────────┐
│ SQLite      │         │ 本地向量库（FAISS /          │
│ 结构化数据  │         │ Chroma / Qdrant 任选）       │
│ 与板块一共享│         │ 存：题目 / 错题 / 知识点切片  │
└─────────────┘         │ 算：cosine 检索 Top-K         │
                        └──────────┬───────────────────┘
                                   │ 仅传「检索片段 + 用户问题」
                                   ▼
                        ┌──────────────────────────────┐
                        │ 云端 LLM（板块一供应商抽象层  │
                        │ 复用，6.4 节）                │
                        │ 负责表达与归纳，不看原始内容  │
                        └──────────────────────────────┘
```

**关键约束**：
- 嵌入（embedding）模型**也优先本地**（如 `bge-small-zh`、`m3e-small`），不传原始内容到云端 embedding 服务；
- 如必须用云端 embedding，需走 6.2 的隐私告知与监护人授权流程；
- 向量库 schema 与 SQLite 同目录部署，便于学生本地备份与销毁（响应未成年人数据可携权）。

### 12.3 核心功能模块

#### 12.3.1 学科知识库（只读浏览）

**目标**：让学生按学科浏览教材级知识结构，作为板块二所有功能的"底图"。

**前端页面**：`/knowledge/<subject>`（学科选择 → 树形/图谱视图）

**核心交互**：
- 知识点以**树 + 图**双视图呈现（树看层级、图看关联）；
- 节点 hover 显示掌握度（颜色梯度：红<40 / 橙 40-70 / 绿>70），点击进入详情；
- 详情页含：定义 / 易错点 / 关联题（与错题本打通）/ 关联概念（与概念图谱打通）。

**MVP 边界**（v2.1 阶段，详见 12.7）：
- 首批接入 **3 个学科**：数学（函数/几何/数列三大主线）、物理（力学/电学）、英语（语法/阅读/写作）；
- 每学科 50-100 个核心知识点，覆盖高中课标 70%；
- 知识库内容**预置**（不开用户上传通道），由内容团队审核后打包发布。

#### 12.3.2 错题本（核心交互入口）

**目标**：学生录入错题 + 错因 + 错题关联到的知识点，长期积累形成"个人错题画像"。

**录入方式**（v2.1 → v2.3 逐步升级）：

| 阶段 | 录入方式 | 数据量 | 体验 |
|---|---|---|---|
| v2.1 | 手动文本输入（题干 + 答案 + 错因文字） | 学生自填 | 录入成本高 |
| v2.2 | 拍照识别（OCR）+ 手动校正 | 中 | 关键路径 |
| v2.3 | 拍照 + 自动归因（LLM 看检索片段 + 错因候选） | 大 | 体验最好 |

**核心字段**（每条错题记录）：

| 字段 | 说明 | 数据出域 |
|---|---|---|
| `errorId` | 主键 | — |
| `subject` | 学科枚举（沿用板块一） | — |
| `knowledgePointIds` | 关联知识点（可多选） | — |
| `rawText` | 错题原文（用户输入） | **不出域** |
| `studentAnswer` | 学生作答 | **不出域** |
| `correctAnswer` | 参考答案 | **不出域** |
| `errorType` | 错因分类（概念不清 / 计算失误 / 审题 / 方法不会 / 其他） | 分类标签可出域 |
| `errorNote` | 学生自填错因说明（自由文本） | **不出域** |
| `vectorId` | rawText 在向量库中的 id | — |
| `status` | unresolved / reviewing / mastered | — |
| `createdAt` / `lastReviewedAt` | 时间戳 | — |

**前端页面**：`/error-book/<subject>`，按学科分组，列表 + 详情，详情含：错题原文（卡片）、关联知识点、错因、复习状态、复习时间线。

**核心交互**：
- 录入流程：选择学科 → 粘贴/拍照错题 → 自动建议关联知识点（向量检索 Top-5 + 知识点匹配打分）→ 学生确认 → 选错因 → 保存；
- 复习流程：间隔复习（艾宾浩斯曲线，由板块一 StateAssessment 的"记忆"维度驱动，v2.2 引入）。

#### 12.3.3 概念关联图谱

**目标**：把知识点之间的关系显式化（前置 / 衍生 / 对比 / 应用），让学生看到"为什么这个题错了还能推出那个题也危险"。

**关系类型**：

| 关系 | 含义 | 示例 |
|---|---|---|
| `prerequisite` | 前置知识（A 学不会则 B 难学） | 函数 ↔ 方程 |
| `derived` | 衍生（A 是 B 的特殊情况或推论） | 一次函数 ↔ 二次函数 |
| `contrast` | 对比（易混点） | 相似 ↔ 全等 |
| `applied_in` | 应用（A 出现在 B 的典型场景） | 三角函数 ↔ 力学振动 |

**存储形态**：
- 知识库预置时一次性生成（由内容团队维护）；
- 学生个人错题触达后，会自动叠加"薄弱路径"高亮（v2.3 引入）。

**前端页面**：`/knowledge/<subject>/graph`，D3/vis-network 渲染。

#### 12.3.4 知识点掌握画像

**目标**：在每个知识点 / 学科维度上量化掌握度，作为板块一 5.2 状态分的"内容补充维度"。

**计算公式**（规则层，AI 不参与具体数值计算）：

```
mastery(knowledgePoint, user) =
  α₁ · (1 - errorRateOnThisPoint)          // 该点错题率
  + α₂ · (avgAccuracyOnExercisesOfThisPoint) // 该点相关练习正确率
  + α₃ · (lastReviewedRecency)              // 最近复习距今越近分越高
  + α₄ · (intervalRetentionScore)            // 间隔复习记忆保持
  - α₅ · (unresolvedErrorCount)              // 未解决错题数扣分
```

权重 `α₁..α₅` 由板块一 5.2 的**同一套调权机制**管理（仅新增"内容维度"权重组），保证规则计算 + AI 调权的一致性。

**输出**：
- 知识点级：`mastery` 0-1 浮点数 + 置信度（数据点 < 3 时置信度低，不展示具体数字，只给定性"数据积累中"）；
- 学科级：所有知识点 mastery 的加权聚合（权重 = 知识点在学科中的占比，如函数 30% / 几何 25% / ...）。

**前端嵌入位置**：
- 知识库节点 hover 提示；
- `/personal-data` 新增"内容掌握"SectionCard（沿用板块一 6 张 SectionCard 风格，板块二再追加 1-2 张）；
- `/study-guide` 选学科时，recommendation 字段补"建议先补：函数·单调性（当前掌握 55%）"。

#### 12.3.5 知识复盘（板块二版"5.4"）

**目标**：区别于板块一 5.4 的"状态与规划维度"复盘，板块二复盘**专注知识内容维度**——"你这段时间哪些知识点在进步、哪些在退步、哪些错题该回炉"。

**触发方式**：
- 用户手动（`/knowledge/<subject>/summary`）；
- 学科级自动周复盘（与板块一 5.4 同周期，但只对**该学科**有 ≥3 条学习/错题记录的用户触发）；
- 摘要 `dimension = "knowledge"` 复用 `summaries` 表，不新建表（沿用板块一预留字段，见 12.4 数据模型）。

**输入数据**（全部不出域，详见 12.6）：
- 该周期内错题本条目（含错因）；
- 该周期内知识点 mastery 变化（用 mastery 增量代替绝对值，避免学生只看到低分焦虑）；
- 板块一 5.2 的 StateAssessment 摘要（仅"学得怎么样"维度，不含具体记录）。

**LLM 调用**：与板块一 5.4 同供应商、同审核层（6.3），但**严格走 12.2.3 的 RAG 路径**——传检索片段 + 画像 + 周期内错题模式（不含 rawText/studentAnswer/correctAnswer/errorNote 原文）。

### 12.4 数据模型（增量新增）

**原则**：与板块一共享 `data.db`（SQLite MVP 阶段）/ 后续可拆库，但不拆表命名空间。所有板块二表加 `kb_` 前缀避免与板块一冲突。

#### 12.4.1 新增 ORM 模型（命名沿用板块一风格）

| 表名 | Python class | 主键前缀 | 字段（简） |
|---|---|---|---|
| `kb_subjects` | `KnowledgeSubject` | — | id, code, name, grade_band, version, enabled |
| `kb_points` | `KnowledgePoint` | `kp_` | id, subject_code, code, name, definition, parent_id, difficulty, exam_weight |
| `kb_point_relations` | `KnowledgePointRelation` | `kpr_` | id, src_id, dst_id, type, weight |
| `kb_errors` | `ErrorRecord` | `err_` | 见 12.3.2 字段表 |
| `kb_error_points` | `ErrorPoint` | — | error_id, point_id, confidence（多对多 + 置信度） |
| `kb_point_mastery` | `PointMastery` | — | user_id, point_id, mastery, sample_size, updated_at（按用户×知识点） |
| `kb_review_logs` | `ReviewLog` | `rvl_` | id, error_id, reviewed_at, recall_correct, interval_days |
| `kb_embeddings` | `EmbeddingRef` | — | vector_id, ref_type('error'/'point'), ref_id, model, dim, created_at |

#### 12.4.2 复用板块一模型

- `summaries`：复用，但 `dimension` 字段新增取值 `knowledge`（板块一 5.4 用 `state_and_plan`）；
- `users`、`goals`、`learning_records`：只读消费，不修改 schema；
- `subjects` 枚举：完全沿用板块一（`math` / `chinese` / ...），不新增学科。

#### 12.4.3 端到端数据流（错题录入 → 画像 → 复盘）

```
用户录入错题
   │
   ▼
[POST /api/v1/error-book]  落 kb_errors（rawText 不出本地）
   │
   ├─→ [本地 embedding] rawText → vector
   │
   ├─→ [kb_embeddings] 存 vectorId
   │
   ├─→ [向量检索 Top-K] → 关联候选知识点
   │
   └─→ [POST /api/v1/knowledge-points/match]  返 Top-5 候选 + 置信度
                                            │
                                            ▼
                            用户确认关联 + 选错因 → 保存
                                            │
                                            ▼
                  [定时 / 触发式] 重算 kb_point_mastery
                                            │
                                            ▼
                  板块一 5.2 状态评估引用 mastery（仅学维度，不出域）
                                            │
                                            ▼
                  学科级周复盘：POST /api/v1/knowledge-summary
                                            │
                                            ▼
                  [本地 RAG 检索 + 出域 LLM 生成 + 6.3 审核] → 写入 summaries（dimension=knowledge）
```

### 12.5 API 设计（增量新增）

**路径前缀**：`/api/v1/`，全部走当前 `current_user` 鉴权（与板块一一致）。

#### 12.5.1 知识库只读

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/knowledge/subjects` | 列出已启用学科（含知识点数量、最近版本） |
| `GET` | `/knowledge/subjects/{code}/points` | 学科下知识点树（parent_id 关系） |
| `GET` | `/knowledge/points/{point_id}` | 单个知识点详情（含定义 / 关联关系） |
| `GET` | `/knowledge/subjects/{code}/graph` | 学科下完整图谱（节点 + 边，前端 D3 用） |
| `GET` | `/knowledge/points/match?text=...` | 给一段文本，返回候选知识点 Top-K（用于错题录入时建议） |

> 全部知识库数据**预置**（`enabled=true` 才可被前端查到），不开放用户写入；如需修订走内容运营后台（v2.3 后另立 PRD）。

#### 12.5.2 错题本

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/error-book?subject=...&status=...&page=...` | 列表（按学科 / 状态过滤） |
| `POST` | `/error-book` | 录入错题（提交后异步：本地 embedding + 知识点匹配 + 入库） |
| `GET` | `/error-book/{error_id}` | 详情 |
| `PATCH` | `/error-book/{error_id}` | 修改错因 / 状态 / 关联知识点 |
| `DELETE` | `/error-book/{error_id}` | 软删（保留 audit 字段，与板块一 goals/archive 风格一致） |
| `POST` | `/error-book/{error_id}/review` | 复习一次（提交 recall_correct，更新 kb_review_logs + 触发 mastery 重算） |

#### 12.5.3 掌握画像

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/mastery/points/{point_id}` | 单知识点 mastery + 置信度 + 样本量 |
| `GET` | `/mastery/subjects/{code}` | 学科级 mastery 聚合 + 子项贡献 |
| `GET` | `/mastery/subjects/{code}/timeline?from=...&to=...` | 学科 mastery 时间序列（用于复盘/个人数据页） |

#### 12.5.4 知识复盘

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/knowledge-summary` | 触发学科级复盘（periodStart/End + subject，必填 subject） |
| `GET` | `/knowledge-summary/{summary_id}` | 详情（生成异步，复用板块一 `summaries` 表，`dimension="knowledge"`） |
| `GET` | `/knowledge-summary?subject=...&from=...&to=...` | 列表（用于 `/summary-review` 页面同时展示两类摘要） |

#### 12.5.5 错误响应与状态码

- 沿用 `docs/openapi.yaml` 0.2 节的 `{ error: { code, message, field? } }` 统一格式；
- 新增错误码（不与板块一冲突）：
  - `KB_TEXT_TOO_LONG`（v1.4 新增）：rawText 超过 4000 字
  - `KB_EMBEDDING_FAILED`（v1.4 新增）：本地 embedding 模型不可用
  - `KB_INSUFFICIENT_MASTERY_DATA`（v1.4 新增）：mastery 样本量 < 3 时不返数值

### 12.6 隐私与数据出域边界（v1.4 统一决策）

**核心原则**（继承并强化 6.2）：

```
板块一（5.1-5.4）                  板块二（12.x）
─────────────                    ─────────────
学习目标 / 备注文本  → 允许出域     错题原文 / 作答 / 错因自述  → 禁止出域
行为数据 / 自评  → 允许出域         错题分类标签 / 错因枚举  → 允许出域
状态分 / 趋势  → 允许出域           知识点 mastery（数值） → 允许出域
-                                检索片段（向量 Top-K 原文） → 禁止出域
                                 嵌入向量本身 → 禁止出域（仅本地）
```

**实现层硬约束**：
1. `LLMProvider` 增加 `data_class` 标签（`state_plan` / `knowledge_aggregated` / `knowledge_raw`），调用方必须显式声明；
2. 知识复盘接口在出域前对 payload 做白名单过滤（只允许 `data_class=knowledge_aggregated` 的字段通过），用 pydantic 模型做 schema 校验兜底；
3. 任何含 `data_class=knowledge_raw` 的字段在 provider 层**直接拒绝序列化**，并在 audit log 记 ERROR；
4. 监护人授权文案新增板块二专款，明确说明"错题原文与作答默认不会发送给第三方 AI，仅用于本地计算"。

**用户控制权**：
- 设置页新增"知识复盘由云端 AI 生成"开关：开启 → 走 RAG + 云端 LLM；关闭 → 走纯本地规则化模板（方案 C 降级，能力降低但隐私最强）；
- 关闭开关时板块二能力**不阻断**，UX 上提示"该模式下列表与复盘会偏模板化"。

### 12.7 阶段化实施计划

| 阶段 | 版本 | 周期 | 范围 | pilot 阶段完成标准 |
|---|---|---|---|---|
| **v2.1** 最小可用 | v2.1.0 | 1.5 个月 | 知识库只读浏览（数学单科 50 点） + 错题本手动文本录入 + 知识点 mastery 基础计算 + 接入 `/personal-data` 1 张 SectionCard | 错题录入到 mastery 出现端到端跑通，1 个种子用户连续使用 2 周无回归 |
| **v2.2** 学科扩展 | v2.2.0 | 1 个月 | 物理 / 英语两科接入（100+200 点） + 拍照 OCR 录入（关键路径）+ 间隔复习（艾宾浩斯）+ 知识复盘（dimension=knowledge）| 3 学科 mastery 端到端 + 1 个种子用户周复盘连续 2 周可读 |
| **v2.3** 体验升级 | v2.3.0 | 1 个月 | 概念图谱可视化 + 错题自动归因（LLM 看检索片段）+ 薄弱路径高亮 + 设置页隐私开关 | 完整 MVP（5 个核心场景可演示）<br>**〔pilot 口径〕**「隐私工程评审通过」在 pilot 阶段降级为团队内部自查并留记录；外部评审签字记入待办 |

> **〔pilot 口径〕** 原表头「上线门」用于正式发布时卡门禁；pilot 阶段不设门禁，改为「完成标准」——某一阶段没达到标准不构成阻塞，记录后继续推进，等正式发布前再统一补齐。

**v1.4 PRD 不实现**，仅为下个迭代做设计准备。立项时需补充：内容团队组建、本地向量库选型（FAISS / Chroma / Qdrant）、本地 embedding 模型选型、`bge-small-zh` 之类模型打包方案。

### 12.8 与板块一衔接的关键接口

| 板块一入口 | 板块二衔接点 | 衔接方式 |
|---|---|---|
| `/personal-data` | 新增"内容掌握"SectionCard（学科 × mastery 进度） | 前端嵌入式 SectionCard，读 `/mastery/subjects/{code}` |
| `/study-guide` | AUTO 推荐补"建议先补：xxx（当前掌握 55%）" | 后端在生成 plan 时查 mastery，按短板排序 |
| `/recommendations`（5.3） | 单次学习后建议新增"内容维度"（与"状态维度"并列） | `recommendations` 表 `scene` 新增 `post_session_knowledge` |
| `/summary-review`（5.4） | 同时展示 `dimension=state_and_plan` 与 `dimension=knowledge` 两类摘要 | 前端按 `dimension` 分组 tab 切换 |
| `/goals` | 学科类目标自动绑定知识点（v2.2 起） | `goal` 增 `point_ids` 字段（可选） |
| `/settings` | 新增"知识复盘 AI 出域"开关 + 隐私说明 | 沿用 6.2 开关模式 |

### 12.9 验收标准（板块二 MVP）

- 三个学科的错题本 + 知识库 + mastery 端到端跑通；
- 错题原文 / 作答 / 自述错因**100% 不出域**（CI 端到端测试断言）；
- 知识复盘生成的内容在板块一 5.4 同等的 6.3 安全审核下通过；
- 板块一 5.4 复盘与板块二知识复盘在 `/summary-review` 页面**入口可清晰区分**（不混在一起）；
- 单用户 100 条错题 + 1000 条 mastery 计算的接口 P95 延迟 < 500ms；
- LLM 全部不可用时板块二核心流程（错题录入、mastery 查看、本地模板化知识复盘）仍可用，不出现空白页。

### 12.10 风险与决策点

| 风险 / 决策 | 影响 | 建议处理 |
|---|---|---|
| 本地 embedding 模型打包进镜像 / 安装包，体积 +50MB | 部署成本 | 提供 `embed_mode=cloud` 开关作为过渡（仍受 6.2 告知约束） |
| 中学全学科 9 个全覆盖内容成本极高 | 内容运营负担 | v2.1 单科试点，验证模式后再扩 |
| 错题 OCR 准确率对中英文/公式参差 | 录入体验 | v2.1 不上 OCR，v2.2 选数学单科试点 |
| mastery 计算公式是否需要 AI 调权 | 与板块一 5.2 调权链路打通 | v2.1 沿用板块一同一调权机制，权重组扩展 1 个"内容维度"组 |
| 用户错题可能含隐私信息（家庭住址、姓名等） | 隐私风险 | 录入前做敏感词检测（沿用 6.2 模式），命中后强制脱敏 / 阻断 |

### 12.11 后续待办（v1.4 评审时确认）

- [ ] 确认本地向量库选型（FAISS 轻量 / Chroma 易用 / Qdrant 集群化）
- [ ] 确认本地 embedding 模型（`bge-small-zh-v1.5` 还是 `m3e-small`）
- [ ] 立项内容运营团队，敲定数学单科 50 知识点 + 关联关系预置清单
- [ ] 出域边界 12.6 强化版审查（重点：错题原文默认不出本地 vs 板块一允许出域的对照）<br>**〔pilot 口径〕**团队内部自查并留记录；正式对外发布前须补法务评审
- [ ] 监护人授权文案 v1.5 改版<br>**〔pilot 口径〕**团队自撰 + 页面标注「学生团队开发，未经专业法律审核」；外部法律意见记入待办

---
